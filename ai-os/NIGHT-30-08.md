# מה נעשה בלילה — 30/08

**כל שינוי עבר `typecheck`, `npm test` ו-`next build` לפני קומיט.**
טסטים: **86 עוברים** (היו 66 בתחילת הלילה).

---

## מה שהלילה הזה נבנה סביבו

הקישור של שחר לא עבד בגלל **תו אחד** שהעתקתי שגוי מצילום מסך — `I` גדולה
במקום `l` קטנה. הוא עבר כל בדיקה שהייתה: מארח נכון, צורה נכונה, `200`.
175 אורחים קיבלו אותו לפני שמישהו ידע.

**וזו לא הייתה תקלה בודדת. זו מחלקה של תקלות** — שום דבר במוצר לא בדק
שקישור שאורח מקבל באמת עובד. הלילה נבנה בעיקר מזה.

---

## 1 · בדיקת קישורים — `src/lib/link-health.ts`

בודקת מה שדפדפן שואל, לא מה ש-curl שואל:

| | הכשל שהיא תופסת |
|---|---|
| תמונת ההזמנה | מחזירה HTML במקום תמונה — הכרטיס לא ייטען |
| קישור RSVP | לא נפתח |
| דף העלאת תמונות | לא נפתח |
| **קבוצת טרמפים** | **`og:title` ריק — וואטסאפ לא מזהה קבוצה** |

**קוד סטטוס הוא הדבר היחיד שלא מבחין בין הזמנה חיה למתה** — ובדיוק לכן
הקישור השגוי שרד.

**שני מקומות:** `/admin/send-preview` לפני ריצה, **וההתראה בריצה האחרונה של
כל לילה.** שותק כשהכול תקין, ו"לא הוגדר" אינו "שבור".

**וגם:** שמירת קישור קבוצה דרך האדמין נבדקת מול וואטסאפ לפני שהיא נכנסת.

## 2 · ממצא 27 — מה הזוג אמר כשנרשם

פאנל באדמין. תהל סימנה **תקציב · לוגיסטיקה · פחד לשכוח · לחץ** ביום שנרשמה,
ואף אחד לא ראה את ארבע המילים האלה מאז. שחר אמרה שיש לה רשימה מוכנה.

## 3 · הדשבורד הראה 139 כשיצאו 247

`wa_runs.sent` ספר רק את קבוצת השליחה של האירוע הנבחר. "מחר מתחתנים",
הגלריה והטרמפים נספרו בנפרד ולא הגיעו לשום סכום. המספר הישן נשמר תחת
`groupSent`.

## 4 · חמישה מסכים בלי דרך להגיע אליהם

סרקתי כל נתיב מול כל קישור. **13 מסכים בלי כניסה, חמישה מהם כלים שאתה צריך:**

```
/admin/event-image      תמונת ההזמנה — בלעדיה השולח מסרב לאירוע שלם
/admin/guests-import    ייבוא רשימה
/admin/whatsapp         שליחה ידנית — הערוץ היחיד של 29 שמטא מסרבת אליהם
/admin/memory           QR וקישורי העלאה
/admin/venues-outreach  8 אולמות שהוספתי — ולא הייתה כניסה
```

כולם חוברו לתפריט. **הדפוס הזה חזר כאן אחת-עשרה פעמים.**

## 5 · ערכת פייסבוק ואינסטגרם — `ai-os/SOCIAL-KIT.md`

ביו לשתי הרשתות, עשרה פוסטים מוכנים להעתקה, וכללי קבוצות. **כל מספר מדוד:**
1,282 מוזמנים, 69% מענה, 73 שלא ניתן היה להשיג.

## 6 · טסטים לקוד המסוכן ביותר

`whatsapp.ts` — 1,140 שורות, לב המערכת, **בלי טסט אחד.** עכשיו 9.
`rides.ts` — 7 טסטים, כולל אחד שמסרב להעביר טלפון של אורח לאורח אחר.

---

## מה שמחכה להחלטה שלך

**🔴 אימות העסק במטא.** עדיין הדבר היחיד שמגביל את כל העסק. 250 → 1,000.
ב-15/12 יש חתונה של 500 מוזמנים שלא ניתנת לביצוע בלעדיו.

**🟡 שני מסלולים עונים על אותה שאלה.** `/api/admin/delivery` (3 דליים, יש
מסך) ו-`/api/admin/coverage` (6 דליים, אין מסך). השני טוב יותר — הוא מבחין
בין "הגיע", "נקרא" ו"לא ידוע", בדיוק ההבחנה שחסרה כל השבוע. **לא ריווטתי מסך
אבחון חי בלילה.**

**🟡 724 שורות קוד שאיש לא מייבא.** `event-progress.ts` (226) מתאר את עצמו
כ"מקור אמת יחיד" ואף אחד לא קורא לו. או לחבר או למחוק.

**🟡 `admin/page.tsx` הוא 5,499 שורות.** עובד, אבל כל שינוי שם הוא סיכון.

---

## שני קומיטים ממתינים לדחיפה

שניהם טסטים בלבד — **אפס שינוי בהתנהגות המערכת.**
